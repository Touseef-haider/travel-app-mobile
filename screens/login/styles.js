import { StyleSheet } from "react-native";

export const Container = StyleSheet.create({
  flex: 1,
  justifyContent: "center",
  alignItems: "center",
});

export const LoginHeading = StyleSheet.create({
  fontSize: 25,
});
